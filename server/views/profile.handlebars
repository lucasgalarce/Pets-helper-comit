<br>
<br>
{{#if animal}}
<div class="container">
    <div class="row">
        <div class="col">
            <h2 class="align-">Nombre: {{animal.name}}</h2>
            <h3>Zona: {{animal.place}}</h3>

            <img class="profile-picture" src="/img/{{animal.profilePic}}" height="200px" alt="">
        </div>
    </div>
</div>

{{else}}
<h2>Animal data unaccessible.</h2>
{{/if}}