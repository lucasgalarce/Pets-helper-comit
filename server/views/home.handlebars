<header>
    <div class="container">
        <div class="row align-items-center">
            <div class="col">
                <h1>Un lugar donde poder reencontrar a tu mascota perdida o adoptar a un animalito en busca de un nuevo
                    hogar
                </h1>
                <!-- Search form -->
                <form class="form-inline d-flex justify-content-end md-form form-sm active-cyan active-cyan-2 mt-2">
                    <i class="fas fa-search" aria-hidden="true"></i>

                </form>
            </div>
        </div>
    </div>
</header>

<div class="animal-list" id="animal-list">
    <h2>Lista de animalitos perdidos:</h2>
    <div class="container">
        <div class="row justify-content-center">
            <input id="nameFilter" class="form-control form-control-sm ml-3 w-25" type="text" placeholder="Buscar"
                aria-label="Search">
            <button class="btn-primary ml-2 rounded" onclick="filter()">Buscar</button>
        </div>
        <div class="row justify-content-center">

            {{#each animals}}
            <div class="card-deck p-3">
                <div class="card" style="width: 18rem;">
                    <img class="card-img-top" src="/img/{{this.animalPic}}" height="200px" alt="Card image">
                    <div class="card-body">
                        <h5 class="card-title">{{this.name}}</h5>
                        <p class="card-text">{{this.info}}.</p>
                        <p class="card-text">Zona: {{this.place}}.</p>
                        <a href="/animal/{{this.objectId}}" class="btn btn-primary">Ver info de {{this.name}}</a>
                    </div>
                </div>
            </div>
            {{/each}}

        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col">
                <div class="Agregar">
                    <a href="/addAnimal">
                        <button class="btn btn-lg btn-warning mb-3 mt-3">Agregar animalito</button>
                    </a>
                </div>
            </div>
        </div>
    </div>

</div>

<script>
    function filter() {
        window.location.href = `/home?nameFilter=` + document.getElementById('nameFilter').value;
    }
</script>